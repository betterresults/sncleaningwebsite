<section class="page-hero page-hero--photo" style="background-image: url('<%= serviceImage(service.slug) %>')">
  <div class="container">
    <span class="eyebrow">Services</span>
    <h1><%= service.title %></h1>
    <p><%= service.short_description %></p>
    <div class="quote-card-badges" style="margin-top:18px;">
      <span>Insured</span>
      <span>DBS-checked</span>
      <span>Own staff</span>
      <span>Re-clean guarantee</span>
    </div>
    <div class="page-hero-actions">
      <a href="/contact?service=<%= encodeURIComponent(service.title) %>" class="btn btn-primary btn-lg">Request a quote</a>
    </div>
  </div>
</section>

<!-- Overview -->
<section class="section">
  <div class="container">
    <div class="service-detail">
      <div class="service-detail-media">
        <% const servicePhoto = (page.images && page.images.length) ? page.images[0] : serviceImage(service.slug); %>
        <img src="<%= servicePhoto %>?v=<%= assetVersion %>" alt="<%= service.title %>" />
      </div>
      <div class="service-detail-text">
        <h2>About This Service</h2>
        <p><%= page.full_description %></p>
      </div>
    </div>
  </div>
</section>

<!-- What's Included, by room -->
<% if (page.included_tasks && page.included_tasks.length) { %>
<section class="section alt-bg">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">What's Included</span>
      <h2>Exactly what we cover</h2>
      <p>A full breakdown of what's included in every <%= service.title.toLowerCase() %> booking.</p>
    </div>
    <div class="grid grid-2">
      <% page.included_tasks.forEach(function(group) { %>
        <div class="card">
          <h3><%= group.room %></h3>
          <ul class="checklist">
            <% group.tasks.forEach(function(task) { %>
              <li><%= task %></li>
            <% }); %>
          </ul>
        </div>
      <% }); %>
    </div>
  </div>
</section>
<% } %>

<!-- Why choose us -->
<section class="section">
  <div class="container grid grid-2 align-center">
    <div>
      <span class="eyebrow">Why SN</span>
      <h2>A local team, not a franchise script</h2>
      <ul class="checklist">
        <li><strong>Our cleaners, every job</strong> — we don't send subcontractors into your home.</li>
        <li><strong>Insured and DBS-checked</strong> — trained staff you can leave a key with.</li>
        <li><strong>Re-clean guarantee</strong> — tell us within 24 hours and we'll come back, free.</li>
        <li><strong>We bring the supplies</strong> — including eco-friendly products on request.</li>
      </ul>
      <a href="/about" class="btn btn-primary">About the company</a>
    </div>
    <div class="stat-card-grid">
      <div class="stat-card"><strong>12</strong><span>Years in business</span></div>
      <div class="stat-card"><strong>London &amp; Essex</strong><span>Where we work</span></div>
      <div class="stat-card"><strong>Own staff</strong><span>No gig workers</span></div>
      <div class="stat-card"><strong>Re-clean</strong><span>If we miss a bit</span></div>
    </div>
  </div>
</section>

<!-- How it works -->
<section class="section steps alt-bg">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">How It Works</span>
      <h2>Booking your <%= service.title.toLowerCase() %> in 3 easy steps</h2>
    </div>
    <div class="grid grid-3 steps-grid">
      <div class="step">
        <div class="step-number">1</div>
        <h3>Request a quote</h3>
        <p>Send the postcode and what you need. We'll quote you — usually the same weekday.</p>
      </div>
      <div class="step">
        <div class="step-number">2</div>
        <h3>Pick a Time</h3>
        <p>Choose a date and time that works for you, including next-day and weekend slots.</p>
      </div>
      <div class="step">
        <div class="step-number">3</div>
        <h3>We Take It From There</h3>
        <p>A vetted, insured cleaner arrives with everything needed to get the job done right.</p>
      </div>
    </div>
  </div>
</section>

<!-- Gallery -->
<% if (page.images && page.images.length > 1) { %>
<section class="section">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">Gallery</span>
      <h2>Recent <%= service.title %> work</h2>
    </div>
    <div class="gallery-grid">
      <% page.images.slice(1).forEach(function(img) { %>
        <img src="<%= img %>" alt="<%= service.title %>" style="width:100%; border-radius:12px;" />
      <% }); %>
    </div>
  </div>
</section>
<% } %>

<!-- Pricing -->
<% if (page.pricing_note) { %>
<section class="section alt-bg">
  <div class="container" style="max-width:820px;">
    <div class="section-heading">
      <span class="eyebrow">Pricing</span>
      <h2>What does it cost?</h2>
    </div>
    <p><%= page.pricing_note %></p>
    <a href="/contact?service=<%= encodeURIComponent(service.title) %>" class="btn btn-primary" style="margin-top:12px;">Get a quote</a>
  </div>
</section>
<% } %>

<!-- Areas we cover -->
<% if (typeof coverageAreas !== 'undefined' && coverageAreas.length) { %>
<section class="section">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">Coverage</span>
      <h2><%= service.title %> across London &amp; Essex</h2>
      <p>We cover the boroughs and towns below — if you don't see yours listed, get in touch and we'll confirm availability.</p>
    </div>
    <div class="area-tags">
      <% coverageAreas.forEach(function(area) { %>
        <span class="area-tag"><%= area %></span>
      <% }); %>
    </div>
  </div>
</section>
<% } %>

<!-- Testimonials -->
<% if (typeof testimonials !== 'undefined' && testimonials.length) { %>
<section class="section testimonials">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">Testimonials</span>
      <h2>What our customers say</h2>
    </div>
    <div class="grid grid-3">
      <% testimonials.forEach(function(t) { %>
        <div class="card testimonial-card">
          <div class="testimonial-top">
            <div class="stars"><%= '★'.repeat(t.rating) %></div>
          </div>
          <p class="quote">"<%= t.quote %>"</p>
          <p class="testimonial-author"><strong><%= t.name %></strong> — <%= t.location %></p>
        </div>
      <% }); %>
    </div>
  </div>
</section>
<% } %>

<!-- FAQs -->
<% if (page.faqs && page.faqs.length) { %>
<section class="section alt-bg">
  <div class="container" style="max-width:820px;">
    <div class="section-heading">
      <span class="eyebrow">FAQs</span>
      <h2>Frequently Asked Questions</h2>
    </div>
    <div class="faq-list">
      <% page.faqs.forEach(function(faq) { %>
        <div class="faq-item" style="margin-bottom:24px;">
          <h3 style="font-size:1.05rem;"><%= faq.question %></h3>
          <p><%= faq.answer %></p>
        </div>
      <% }); %>
    </div>
  </div>
</section>
<% } %>

<!-- Related services -->
<% if (typeof relatedServices !== 'undefined' && relatedServices.length) { %>
<section class="section">
  <div class="container">
    <div class="section-heading">
      <span class="eyebrow">Related</span>
      <h2>You might also need</h2>
    </div>
    <div class="grid grid-3">
      <% relatedServices.forEach(function(s) { %>
        <a href="/<%= s.slug %>" class="card service-card">
          <div class="service-card-photo">
            <img src="<%= serviceImage(s.slug) %>?v=<%= assetVersion %>" alt="<%= s.title %>" />
          </div>
          <div class="service-card-body">
            <h3><%= s.title %></h3>
            <p><%= s.short_description %></p>
            <span class="card-link">What's included</span>
          </div>
        </a>
      <% }); %>
    </div>
  </div>
</section>
<% } %>

<section class="section cta-band">
  <div class="container cta-band-inner">
    <div>
      <h2>Ready to book <%= service.title.toLowerCase() %>?</h2>
      <p>Tell us about your space and we'll recommend the right schedule.</p>
    </div>
    <a href="/contact?service=<%= encodeURIComponent(service.title) %>" class="btn btn-primary btn-lg">Request a quote</a>
  </div>
</section>
